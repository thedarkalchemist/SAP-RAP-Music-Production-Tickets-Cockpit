projection implementation in class ZBP_C_TMUSIC_COCKPIT unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_TMUSIC_COCKPIT alias ZcTmusicCockpit
extensible
use etag
{
  use create;
  use update;
  use delete;

  side effects { field StatusID affects field StatusDescription; }
  use action sendToTelegram;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}